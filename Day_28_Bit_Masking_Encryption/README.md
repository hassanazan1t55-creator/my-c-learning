### Day 28: Bit Masking and XOR Encryption Basics
* **Folder:** `Day_28_Bit_Masking_Encryption`
* **File:** `xor_cipher.c`
* **What I Learnt:**
  * Understand Bit Masking concepts using Bitwise OR (`|`) to set flags and AND (`&`) to check security permissions.
    > Security permissions set karne aur check karne ke liye bit masking ka rule samjha.
  * Explore ASCII mapping (`'A'` = 65) and how bitwise math processes characters as integers underneath.
    > Characters ke piche chupi ASCII mathematical processing ko closely observe kiya.
  * Master XOR Encryption algorithms to obfuscate plain numbers into raw unprintable cipher tags.
    > XOR logic se code hide karne aur security tools ko bypass karne ki core baseline seekhi.
  * Implement full reversible symmetric decryption loops using a single matching secret key.
    > Same key se dynamic data ko bina corruption ke secure decode karna seekha.
