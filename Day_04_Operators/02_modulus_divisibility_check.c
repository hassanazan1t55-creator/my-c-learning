#include <stdio.h>

int main() {
    int a = 2344234;

    // Modulus operator (%) returns the remainder of division
    printf("The remainder of %d divided by 97 is: %d\n", a, a % 97);

    // If output returns 0 = divisible
    // Else = not divisible

    return 0;
}
