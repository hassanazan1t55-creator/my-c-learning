#include <stdio.h>

int main() {
    // Syntax: condition ? expression_if_true : expression_if_false
    int a = 35;
    int b = 32;

    a > b ? printf("A is greater\n") : printf("B is greater\n");

    return 0;
}
